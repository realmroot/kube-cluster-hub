apiVersion: v1
kind: Namespace
metadata:
  name: cluster-access-system
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cluster-access-control-plane-data
  namespace: cluster-access-system
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-access-control-plane
  namespace: cluster-access-system
data:
  GATEWAY_PUBLIC_URL: https://clusters.example.com
  GATEWAY_INVENTORY_ACCESS_URL: https://clusters.example.com
  OIDC_ISSUER: https://identity.example.com
  OIDC_AUDIENCE: kubernetes-client
  OIDC_GROUPS_CLAIM: groups
  CATALOG_ADMIN_GROUPS: platform-admins
  RESOURCE_SERVER_URL: https://clusters.example.com/api/agent
  RESOURCE_SERVER_ISSUER: https://identity.example.com
  RESOURCE_SERVER_AUTHORIZED_CLIENT_IDS: authorized-toolbox-client
  RESOURCE_SERVER_JWT_ALGORITHMS: RS256
  DISPATCH_ISSUER: https://clusters.example.com
  DISPATCH_AUDIENCE: cluster-access-connector
  INVENTORY_CLUSTER_ID: management
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cluster-access-control-plane
  namespace: cluster-access-system
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: cluster-access-control-plane
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cluster-access-control-plane
    spec:
      securityContext:
        runAsNonRoot: true
        fsGroup: 65532
      containers:
        - name: control-plane
          image: cluster-access-control-plane:dev
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: cluster-access-control-plane
            - secretRef:
                name: cluster-access-control-plane-secrets
          env:
            - name: PORT
              value: "8080"
            - name: GATEWAY_DATABASE_DSN
              value: /data/control-plane.db
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet: {path: /readyz, port: http}
          livenessProbe:
            httpGet: {path: /healthz, port: http}
          resources:
            requests: {cpu: 25m, memory: 64Mi}
            limits: {cpu: "1", memory: 256Mi}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: [ALL]}
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: cluster-access-control-plane-data
---
apiVersion: v1
kind: Service
metadata:
  name: cluster-access-control-plane
  namespace: cluster-access-system
spec:
  selector:
    app.kubernetes.io/name: cluster-access-control-plane
  ports:
    - name: http
      port: 8080
      targetPort: http
